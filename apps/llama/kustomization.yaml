apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - secret.yaml
  - auth-proxy.yaml
  - ingress.yaml
  - pvc-ssd.yaml
  - pvc-kokoro-ssd.yaml
  - deployment.yaml
  - kokoro.yaml
configMapGenerator:
  - name: llama-swap
    namespace: llama
    files:
      - config.yaml=configs/config.yaml
