---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: llama
  name: models
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi
  storageClassName: mayastor-single-hdd
